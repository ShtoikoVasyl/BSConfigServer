spring:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    generate-ddl: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

eureka:
  instance:
    prefer-ip-address: true

account-service:
  default-pin: 1111